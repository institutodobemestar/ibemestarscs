---
interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={['mx-auto w-full max-w-6xl px-[var(--space-3)] md:px-[var(--space-4)]', className]}>
	<slot />
</div>
